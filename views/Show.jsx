const React = require('react');
const Default = require('./Default');

function Show({ place }) {
    return (
        <Default>
            <main>

            </main>
        </Default>
    )
};

module.exports = Show;
